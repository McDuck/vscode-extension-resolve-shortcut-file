{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Build, Package & Install (F5)",
      "type": "PowerShell",
      "request": "launch",
      "script": "${workspaceFolder}/build.ps1",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
